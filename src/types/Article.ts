export interface Article {
    id: number;
    title: string;
    published_at: string;
    summary: string;
    image_url: string;
    url: string;
    source: string;
}

export default Article;

